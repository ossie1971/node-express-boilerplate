extends layout
block content
    .col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.main
        h2 Error        
        if message
            p #{message}
        if stack
            p Stack Trace
            p #{stack}
            
